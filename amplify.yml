version: 1
frontend:
  phases:
    preBuild:
      commands:
        - eslint --fix
        - npm ci
    build:
      commands:
        - amplifyPush --simple
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
